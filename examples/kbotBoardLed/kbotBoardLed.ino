/*
 * Пример использования светодиодов на плате KBot
 *
 * Подключение:
 * 1. Загрузите скетч на M5Stack Core2
 * 2. Светодиод DA9 загорится красным цветом
 */

#include <KBot.h>  // Подключаем библиотеку для работы с роботом KBot

KBot bot;  // Создаем объект робота

void setup(){
    bot.begin();  // Инициализируем робота (запускаем все датчики и моторы)
}

void loop(){
    bot.update();  // Обновляем состояние всех датчиков (вызываем каждый цикл!)

    // Включаем светодиод DA9 красным цветом
    // DA9 - это название конкретного светодиода на плате KBot
    bot.kbotBoardLed.color(Led::Name::DA9, Led::Color::RED);
}